[pytest]
# 사용자 정의 마커 등록 — 등록 안 된 마커는 PytestUnknownMarkWarning 발생.
markers =
    golden: 골든 스냅샷 회귀 검증 (실제 LLM 호출 없이 저장된 JSON 만 검증). CI 에서 제외하려면 -m "not golden". 스냅샷 생성은 scripts/run_bullbear_golden.py.
