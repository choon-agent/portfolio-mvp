# 로컬 개발·스크립트 실행용 의존성.
# requirements.txt (Lambda 번들) 에 더해 로컬에서만 필요한 패키지.
#
# 사용:
#   pip install -r requirements.txt -r requirements-dev.txt

# Lambda 런타임이 기본 제공하므로 번들하지 않음 (zip 크기 절약).
# 로컬 스크립트(scripts/backfill_ohlcv.py) 는 boto3 직접 호출.
boto3>=1.34,<2.0

# 테스트 (Lambda 번들 제외 — deploy_lambda.sh 가 tests/ 디렉토리와 dev deps 모두 자동 제외).
pytest>=8.0,<9.0

# Bull/Bear 응답 품질 평가 (G-Eval 기반, judge = Sonnet 4.6).
# 로컬·CI 전용 — Lambda 번들 제외. evaluation/ 패키지 import 는 lazy 하므로
# SDK 미설치 환경에서도 agents.bull_bear 의 다른 모듈은 영향 없음.
# M3+ 운영 모니터링 단계에서 Lambda 도입 시 requirements.txt 로 승격 검토.
deepeval>=2.0,<3.0
# 4단계 optimizer 로컬 개발·테스트용 (Lambda 는 컨테이너 이미지에 별도 고정 — 04 §7.2)
PyPortfolioOpt>=1.6,<2.0
