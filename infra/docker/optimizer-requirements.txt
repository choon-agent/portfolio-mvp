# run_optimizer 컨테이너 이미지 전용 (04 §7.2) — zip Lambda 번들과 분리.
# numpy/scipy/cvxpy 계열은 50MB zip 한계 초과 → 컨테이너 방침 (infra/README).
pydantic>=2.6,<3.0
pyarrow>=15.0,<20.0
boto3>=1.34,<2.0
PyPortfolioOpt>=1.6,<2.0
# pypfopt(skbase) 의 미선언 런타임 의존성 — 없으면 import 시
# ModuleNotFoundError (빌드 스모크로 검출, 2026-08-11)
packaging>=23.0
