# run_rebalancer 컨테이너 이미지 전용 (05 §7.2) — zip Lambda 번들과 분리.
# pyarrow 는 zip 슬리밍 사고 전례(302f3cb)로 컨테이너 방침 (infra/README).
# cvxpy/PyPortfolioOpt 불필요 — optimizer.schemas(순수 pydantic)만 의존.
pydantic>=2.6,<3.0
pyarrow>=15.0,<20.0
boto3>=1.34,<2.0
pandas>=2.0,<3.0
